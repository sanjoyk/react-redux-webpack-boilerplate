import { BookAction } from "../app-constants/index.js";
const bookSettingsDefault = {
};
const bookSettings = (state = bookSettingsDefault, action) => {
  switch (action.type) {
  default: return state;
  }
};
export default bookSettings;